name: Java CI with Maven

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch: {}

permissions: 
  id-token: write
  contents: read

env:
  AZURE_RG: myfirstresource              # <-- PUT YOUR REAL RG NAME
  AZURE_LOCATION: eastus                 # region of your RG/plan/app
  AZURE_PLAN: tomcat-linux-plan          # app service plan name
  AZURE_WEBAPP: QA-deom-app              # web app name (Linux)
  
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven
      - name: Build with Maven
        run: mvn -B -DskipTests package --file pom.xml
      - name: Upload WAR artifact
        uses: actions/upload-artifact@v4
        with:
          name: devops-tomcat-demo
          path: target/*.war
      - run: |
          echo "OWNER/REPO = ${GITHUB_REPOSITORY}"
          echo "REF        = ${GITHUB_REF}"


  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Azure login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      - uses: actions/download-artifact@v4
        with:
          name: devops-tomcat-demo

      - name: Deploy WAR to Azure Web App
        run: |
          WAR=$(ls *.war | head -n1)
          az webapp deploy \
            --resource-group "<YOUR_RG_NAME>" \
            --name "<YOUR_WEBAPP_NAME>" \
            --src-path "$WAR" \
            --type war \
            --async false
